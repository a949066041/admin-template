{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "prepare:build": {
      "dependsOn": ["^prepare:build"],
      "inputs": ["src/**/*.ts", "svg/**/*.svg"],
      "outputs": ["dist/**", "src/**/*.js"]
    }
  }
}
